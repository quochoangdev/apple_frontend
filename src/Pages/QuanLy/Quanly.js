import classNames from "classnames/bind";

import styles from "./QuanLy.module.scss";

const cx = classNames.bind(styles);

const Quanly = () => {
  return <div className={cx("")}>Quanly</div>;
};

export default Quanly;
